Options -Indexes

# Block direct access to PHP if someone uploads something nasty
<FilesMatch "\.(php|phtml|phar)$">
  Deny from all
</FilesMatch>

# Allow only common invoice image types (basic safety)
<FilesMatch "\.(jpg|jpeg|png|webp|pdf)$">
  Require all granted
</FilesMatch>